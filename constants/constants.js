const OK_CODE = 200;
const CREATED_CODE = 201;

const ID_LENGTH = 24;

const VALID_ERR = 400;
const DATA_ERR = 404;
const DEFAULT_ERR = 500;

module.exports = {
  OK_CODE,
  CREATED_CODE,
  ID_LENGTH,
  VALID_ERR,
  DATA_ERR,
  DEFAULT_ERR,
};
